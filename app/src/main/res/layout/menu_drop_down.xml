<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="190dp"
    android:layout_height="wrap_content"
    android:background="@color/title_end_color">

    <TextView
        android:id="@+id/menu_drop_down_scan"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/titlebar_height"
        android:drawableLeft="@mipmap/scan"
        android:drawablePadding="12dp"
        android:gravity="center"
        android:paddingEnd="@dimen/common_leftorright_padding"
        android:paddingStart="@dimen/common_leftorright_padding"
        android:text="扫一扫"
        android:textColor="@color/background"
        android:textSize="16sp" />
</android.support.constraint.ConstraintLayout>